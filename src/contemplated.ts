/**
 * The base class for the Contemplated module
 */
export default class Contemplated {
  constructor() {
    this.run();
  }

  /**
   * Runs the module
   * @return returns a bool of true to confirm it ran.
   */
  run(): boolean {
    return true;
  }
}
